Big Zip
Author: LT 'syreal' Jones

Description
Unzip this archive and find the flag.

Too many text files and folders to try manually 
flag place isnt known 

command 
`grep -R -o 'picoCTF{[^}]*}' .`

What this does

-R → recursive (all subdirs)

-o → only print the match

Pattern → extract the flag

. → start here